import React from 'react';

import classes from './Logo.module.scss';

const Logo = function () {
  return (
    <a href="#" className={classes.logo}>
      <div></div>
    </a>
  );
};

export default Logo;
